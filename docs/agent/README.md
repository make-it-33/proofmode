# ProofMode agent operating system

This directory and `docs/handoff/` let a strong coding agent continue without the original conversation and without diluting the product.

Read in order: root `AGENTS.md`; `HANDOFF.md`; `APPROVALS.md`; `../product/APP_MASTER_SPEC.md`; `../product/PAGE_AND_SECTION_SPECS.md`; `../roadmap/PRODUCT_IMPLEMENTATION_PLAN.md`; `QUALITY_BAR.md`; `../handoff/README.md`; then `RUNBOOK.md`, `WORKFLOW.md`, `TOOLING.md`, and `RUN_LOG.md`.

Root `agent.md` is a compatibility pointer only. `HANDOFF.md` is the current operational snapshot. `docs/handoff/` contains deeper continuity maps. Update `HANDOFF.md` after every meaningful run and append the same run ID to `RUN_LOG.md`.

The owner delegated routine implementation, quality repair, branch updates, documentation, PR readiness, and green merging inside the approved blueprint. Do not repeatedly ask permission, but stop at explicit data/provider/cost/legal/youth-safety/irreversible gates.

No competition is part of the active product or V1/V2 roadmap. Historical Arena/rating records are superseded and **Not approved for production**.
